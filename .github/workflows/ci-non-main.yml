name: CI Non Main

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches-ignore:
      - main

jobs:
  build:
    uses: ./.github/workflows/build-template.yml
    secrets: inherit
